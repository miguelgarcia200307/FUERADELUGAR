import { writeFile } from 'node:fs/promises';
import { join } from 'node:path';
import { tmpdir } from 'node:os';

const tabs=await fetch('http://localhost:9223/json/list').then(response=>response.json());
const page=tabs.find(tab=>tab.type==='page');
if(!page)throw new Error('No se encontró una pestaña para QA.');
const socket=new WebSocket(page.webSocketDebuggerUrl);
await new Promise((resolve,reject)=>{socket.addEventListener('open',resolve,{once:true});socket.addEventListener('error',reject,{once:true});});
let id=0;const pending=new Map();const errors=[];
socket.addEventListener('message',event=>{const message=JSON.parse(event.data);if(message.id&&pending.has(message.id)){const task=pending.get(message.id);pending.delete(message.id);message.error?task.reject(new Error(message.error.message)):task.resolve(message.result);}if(message.method==='Runtime.exceptionThrown')errors.push(message.params.exceptionDetails?.exception?.description||message.params.exceptionDetails?.text);});
const command=(method,params={})=>new Promise((resolve,reject)=>{const requestId=++id;pending.set(requestId,{resolve,reject});socket.send(JSON.stringify({id:requestId,method,params}));});
const evaluate=expression=>command('Runtime.evaluate',{expression,awaitPromise:true,returnByValue:true}).then(result=>{if(result.exceptionDetails)throw new Error(result.exceptionDetails.exception?.description||result.exceptionDetails.text);return result.result.value;});
const wait=milliseconds=>new Promise(resolve=>setTimeout(resolve,milliseconds));
const screenshot=async name=>{const path=join(tmpdir(),name);const result=await command('Page.captureScreenshot',{format:'png',captureBeyondViewport:false});await writeFile(path,Buffer.from(result.data,'base64'));return path;};

await command('Runtime.enable');await command('Page.enable');
await command('Emulation.setDeviceMetricsOverride',{width:390,height:844,deviceScaleFactor:1,mobile:true});
await evaluate(`history.replaceState({},'',location.pathname+'?section=promotions');document.querySelector('[data-section="promotions"]').click()`);await wait(250);
const mobile=await evaluate(`(() => {const row=document.querySelector('.promotion-row');const action=row?.querySelector('[data-edit-promo]');const menu=row?.querySelector('.action-menu summary');return{path:location.href,title:document.querySelector('.admin-heading h1')?.textContent,newPromotion:document.querySelector('#new-promotion')?.getBoundingClientRect().height,search:document.querySelector('#promotion-search')?.getBoundingClientRect().height,chips:document.querySelectorAll('[data-promo-status]').length,rows:document.querySelectorAll('.promotion-row').length,action:{text:action?.textContent,display:action?getComputedStyle(action).display:null,height:action?.getBoundingClientRect().height},menu:{display:menu?getComputedStyle(menu).display:null,height:menu?.getBoundingClientRect().height},overflow:document.documentElement.scrollWidth-innerWidth};})()`);
const mobileShot=await screenshot('fdl-admin-promotions-390.png');

const createFlow=await evaluate(`(async()=>{document.querySelector('#new-promotion').click();await new Promise(resolve=>setTimeout(resolve,20));const picker={open:Boolean(document.querySelector('.promotion-picker')),items:document.querySelectorAll('[data-picker-product]').length};const item=[...document.querySelectorAll('[data-picker-product]')].find(row=>row.querySelector('button').textContent.includes('Seleccionar'));item.querySelector('button').click();await new Promise(resolve=>setTimeout(resolve,20));const form=document.querySelector('.promotion-form');const price=form.elements.promo_price;price.value=form.querySelector('.promotion-form__product em').textContent.match(/[\\d.]+/)[0].replaceAll('.','');form.requestSubmit();await new Promise(resolve=>setTimeout(resolve,20));const validation=form.querySelector('[data-promo-form-error]').textContent;price.value='59000';price.dispatchEvent(new Event('input',{bubbles:true}));const start=new Date(Date.now()+86400000).toISOString().slice(0,16);const end=new Date(Date.now()+172800000).toISOString().slice(0,16);form.elements.promo_start.value=start;form.elements.promo_end.value=end;form.requestSubmit();await new Promise(resolve=>setTimeout(resolve,120));return{picker,validation,closed:!document.querySelector('.promotion-form'),toast:document.querySelector('.toast:last-child')?.textContent,scheduled:[...document.querySelectorAll('.status-pill')].some(node=>node.textContent.trim()==='Programada')};})()`);

const filters=await evaluate(`(async()=>{const inactive=document.querySelector('[data-promo-status="inactive"]');inactive.click();await new Promise(resolve=>setTimeout(resolve,20));const empty=document.querySelector('.promotion-empty h3')?.textContent;document.querySelector('[data-promo-status="active"]').click();const activeRows=document.querySelectorAll('.promotion-row').length;const search=document.querySelector('#promotion-search');search.value='Argentina';search.dispatchEvent(new Event('input',{bubbles:true}));await new Promise(resolve=>setTimeout(resolve,240));return{empty,activeRows,searched:document.querySelectorAll('.promotion-row').length,url:location.search};})()`);

const actions=await evaluate(`(async()=>{const search=document.querySelector('#promotion-search');search.value='';search.dispatchEvent(new Event('input',{bubbles:true}));document.querySelector('[data-promo-status="active"]').click();await new Promise(resolve=>setTimeout(resolve,240));let row=document.querySelector('.promotion-row');const name=row.querySelector('.promotion-product strong').textContent;const summary=row.querySelector('.action-menu summary');summary.click();await new Promise(resolve=>setTimeout(resolve,0));const menuOpened=row.querySelector('.action-menu').open&&summary.getAttribute('aria-expanded')==='true';row.querySelector('[data-promo-disable]').click();await new Promise(resolve=>setTimeout(resolve,120));document.querySelector('[data-promo-status="inactive"]').click();row=[...document.querySelectorAll('.promotion-row')].find(item=>item.querySelector('.promotion-product strong').textContent===name);const disabled=row?.querySelector('.status-pill').textContent.trim()==='Inactiva';row.querySelector('.action-menu summary').click();row.querySelector('[data-remove-promo]').click();await new Promise(resolve=>setTimeout(resolve,20));const confirmation=document.querySelector('.modal')?.innerText.includes('El producto continuará disponible con su precio normal.');document.querySelector('[data-confirm]').click();await new Promise(resolve=>setTimeout(resolve,120));document.querySelector('[data-promo-status="none"]').click();search.value=name;search.dispatchEvent(new Event('input',{bubbles:true}));await new Promise(resolve=>setTimeout(resolve,240));row=document.querySelector('.promotion-row');return{name,menuOpened,disabled,confirmation,productPreserved:Boolean(row),nowWithoutPromotion:row?.querySelector('.status-pill').textContent.trim()==='Sin promoción'};})()`);

const viewports=[];
await evaluate(`document.querySelector('#promotion-search').value='';document.querySelector('#promotion-search').dispatchEvent(new Event('input',{bubbles:true}));document.querySelector('[data-promo-status="all"]').click()`);await wait(240);
for(const [width,height] of [[320,568],[390,844],[768,1024],[1024,768],[1440,900],[1920,1080]]){await command('Emulation.setDeviceMetricsOverride',{width,height,deviceScaleFactor:1,mobile:width<800});await wait(50);viewports.push(await evaluate(`(() => {const action=document.querySelector('.promotion-row [data-edit-promo]');return{size:[innerWidth,innerHeight],overflow:Math.max(0,document.documentElement.scrollWidth-innerWidth),actionVisible:Boolean(action&&getComputedStyle(action).display!=='none'&&action.getBoundingClientRect().height>=38),tableHead:getComputedStyle(document.querySelector('.promotion-table__head')).display};})()`));}
const desktopShot=await screenshot('fdl-admin-promotions-1920.png');
await command('Emulation.setDeviceMetricsOverride',{width:320,height:568,deviceScaleFactor:1,mobile:true});await wait(100);const fresh320=await evaluate(`(() => ({viewport:[innerWidth,innerHeight],document:[document.documentElement.scrollWidth,document.documentElement.scrollHeight],overflow:Math.max(0,document.documentElement.scrollWidth-innerWidth),actionVisible:document.querySelector('.promotion-row [data-edit-promo]')?.getBoundingClientRect().height>=38,newPromotionVisible:document.querySelector('#new-promotion')?.getBoundingClientRect().height>=44}))()`);
await evaluate(`document.querySelector('#new-promotion').click();document.querySelector('[data-picker-product] button').click()`);await wait(50);const mobileModal=await evaluate(`(() => {const modal=document.querySelector('.modal--promotion');const footer=document.querySelector('.promotion-form__footer');const head=modal.querySelector('.modal__head');return{open:Boolean(modal),size:[Math.round(modal.getBoundingClientRect().width),Math.round(modal.getBoundingClientRect().height)],headerVisible:head.getBoundingClientRect().top>=0,footerVisible:footer.getBoundingClientRect().bottom<=innerHeight+1,footerButtons:footer.querySelectorAll('button').length,overflow:Math.max(0,document.documentElement.scrollWidth-innerWidth)};})()`);const modalShot=await screenshot('fdl-admin-promotion-form-320.png');
console.log(JSON.stringify({mobile,createFlow,filters,actions,viewports,fresh320,mobileModal,errors,screenshots:{mobileShot,desktopShot,modalShot}},null,2));socket.close();
